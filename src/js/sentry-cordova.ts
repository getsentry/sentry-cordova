export { CordovaOptions } from './backend';
export { CordovaClient } from './client';
export {
  addBreadcrumb,
  captureEvent,
  captureException,
  captureMessage,
  configureScope,
  init,
  setDist,
  setRelease,
  setVersion,
} from './sdk';
export { Hub, Scope } from '@sentry/hub';
