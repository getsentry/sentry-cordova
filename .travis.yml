branches:
  only:
    - master

sudo: false
language: node_js
node_js: '8'
dist: trusty
cache: yarn
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - export PATH="$HOME/.yarn/bin:$PATH"
script: yarn test